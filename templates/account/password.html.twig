{% extends 'base.html.twig' %}
{% block title %}Changer mon mot de passe{% endblock %}
{% block body %}
<h1 class="mb-3">Changer mon mot de passe</h1>
<div class="card">
  <div class="card-body">
    {{ form_start(form) }}
      {{ form_widget(form) }}
      <button class="btn btn-primary mt-2">Mettre à jour</button>
    {{ form_end(form) }}
  </div>
</div>
{% endblock %}

